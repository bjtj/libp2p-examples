# Run a js-libp2p node

<https://libp2p.io/docs/getting-started-javascript/>

NOTE) use `@multiformats/multiaddr` instead of `multiaddr`

## Run

e.g.) server

``` shell
$ pnpm run dev
```

e.g.) client

``` shell
$ pnpm run dev /ip4/127.0.0.1/tcp/60373
```
